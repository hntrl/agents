// First primitive, named after the project's purpose. One concept per file.
// Follow the typescript-best-practices skill: strict types, named exports,
// TSDoc on public APIs, no `any`, narrow `unknown` at boundaries.

/**
 * Replace this with the project's first real primitive.
 *
 * @param value The input to process.
 * @returns The processed result.
 */
export function __FEATURE_FN__<T>(value: T): T {
  return value;
}
