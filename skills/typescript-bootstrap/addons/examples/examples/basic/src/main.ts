// Minimal end-to-end usage of the library's public API.
// Swap __FEATURE_FN__ for the real first export.
import { __FEATURE_FN__ } from "__PKG_NAME__";

const result = __FEATURE_FN__("hello from the basic example");
console.log(result);
