# basic

Minimal end-to-end usage of `__PKG_NAME__`.

## Run

```bash
pnpm install
pnpm start
```

`src/main.ts` imports the library's public API and runs it. Use this as the
template for new examples (see `../README.md` → "Contributing an example").
